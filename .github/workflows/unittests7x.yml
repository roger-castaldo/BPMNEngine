name: .NET-Test-7x

on:
  push:
    branches: [ 'Version2' ]
  pull_request:
    branches: [ 'Version2' ]

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v3
    - name: Setup .NET
      uses: actions/setup-dotnet@v2
      with:
        dotnet-version: |
          7.0.x
    - name: Restore dependencies
      run: dotnet restore UnitTest
    - name: Build-7.0
      run: dotnet build --framework net7.0 --no-restore UnitTest
    - name: Test-7.0
      run: dotnet test --framework net7.0 --no-build --verbosity normal --filter "ClassName!=UnitTest.ImageTests" UnitTest